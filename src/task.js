const Task = (title, description, dueDate, project, priority) => {
  let _isCompleted = false;
}

const Project = (name) => {
  let _tasks = [];

  const getName = () => name;

  return {
    getName
  };
}

export {Task, Project};